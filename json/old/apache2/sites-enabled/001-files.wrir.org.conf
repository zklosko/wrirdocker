<VirtualHost *:80>
        ServerName files.wrir.org

        DocumentRoot /var/www/web80/
        DirectoryIndex /cgi-bin/od
	Options -Indexes
        
        Alias /shows /var/www/web1/storage/ShowArchive 
        <Directory /var/www/web1/storage/ShowArchive>
          Options -Indexes
        </Directory>

        # CGI Directory
        ScriptAlias /cgi-bin/ /usr/local/cgi-bin/web80/
        <Location /cgi-bin>
                Options +ExecCGI
		Require all granted
        </Location>

        # Logfiles
        ErrorLog /var/log/apache2/web80/error.log
        CustomLog /var/log/apache2/web80/access.log combined
</VirtualHost>

